class Solution:
    def findPoisonedDuration(self, timeSeries, duration):

        # Interval patterns
        # Teemo Attacking (495)
        # Merge Intervals (56)
        # Insert Interval (57)
        # Non-overlapping Intervals (435)
        # Meeting Rooms / Meeting Rooms II


            





s = Solution()
# print(s.findPoisonedDuration(timeSeries=[1,2,3,4,5,6,7,8,9], duration=0))
# print(s.findPoisonedDuration(timeSeries = [1,4], duration = 2))
# print(s.findPoisonedDuration(timeSeries = [1,2], duration = 2))
s.findPoisonedDuration(timeSeries=[1,2,3,4,5,6,7,8,9], duration=100000)
        